models:
  Procurement:
    title: string
    supplier: string
    description: text nullable
    total_amount: decimal:10,2
    status: string
    timestamps: true

controllers:
  ProcurementRequestController:
    resource: web
